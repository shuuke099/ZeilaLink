// Next.js permits global stylesheets to be imported for their side effects
// from the root layout. This declaration also makes that import visible to
// TypeScript language-service versions that enable side-effect import checks.
declare module '*.css';
